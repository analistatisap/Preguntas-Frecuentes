# Usa una imagen oficial de Nginx como base
FROM nginx:alpine

# Elimina la configuración por defecto de Nginx
RUN rm /etc/nginx/conf.d/default.conf

# Copia tu archivo de configuración personalizado de Nginx al contenedor
# Crearemos este archivo 'nginx.conf' en el siguiente paso
COPY nginx.conf /etc/nginx/conf.d/

# Copia los archivos estáticos construidos de tu frontend (la carpeta 'dist')
# a la ubicación donde Nginx espera servir los archivos.
# Asegúrate de que 'dist' es el nombre correcto de tu carpeta de build.
COPY dist/ /usr/share/nginx/html/

# Expone el puerto 80 (HTTP)
EXPOSE 80

# Comando para iniciar Nginx cuando el contenedor arranque
CMD ["nginx", "-g", "daemon off;"]